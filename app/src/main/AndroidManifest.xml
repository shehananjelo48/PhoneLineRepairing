<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shehan.com.sltphonelinerepairing">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/cloud"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TEST"
            android:theme="@style/noActionbarTheme">
            <!--<intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>-->
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity
            android:name=".TestPage"
            android:theme="@style/noActionbarTheme">
        </activity>
        <activity android:name=".Finish" android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Fault_Menu" android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Rate" android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Cabinet_No" android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Admin.Register_Admin" android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Client_Menu"
            android:theme="@style/noActionbarTheme"/>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Add_Client" android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Technician_menu"
            android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Register_Technician" android:theme="@style/noActionbarTheme"/>
        <activity android:name=".Admin.Admin_menu"
            android:theme="@style/noActionbarTheme"/>
        <activity
            android:name=".Admin.Admin_Login"
            android:parentActivityName=".MainActivity"
            android:theme="@style/noActionbarTheme" />
        <activity
            android:name=".Technician_login"
            android:theme="@style/noActionbarTheme"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Client_Login"
            android:parentActivityName=".MainActivity"
            android:theme="@style/noActionbarTheme"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/noActionbarTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>